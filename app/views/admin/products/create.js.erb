<% if @product.errors.any? %>

$("#modalBody").html("<%= j render 'form' %>");

<% else %>

$("#commonModal").modal("hide");
$("#productsTable").html("<%= j render 'table' %>");
$("#flash").html("<%= j render 'shared/flash' %>");

<% end %>