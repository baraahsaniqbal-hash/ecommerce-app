<% if @category.errors.any? %>
  $("#modalBody").html("<%= escape_javascript render 'form', category: @category %>");
<% else %>
  $("#commonModal").modal("hide");
  reloadTable();
  $("#flash").html("<%= escape_javascript render 'shared/flash' %>");
<% end %>
